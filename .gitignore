# Engine intermediate review snapshots — agent uses .tmp/ for these
.tmp/
review_*.json
_findings.json
*_baseline.json
*_after_*.json
*_before_*.json
# Typo path artifacts: deveriam estar em .tmp/review_* mas saíram com prefixo
# .tmpreview_ na raiz. Mesma semântica — descartável entre sessões.
.tmpreview_*

# Ad-hoc LLM evaluation outputs (log message quality benchmarks etc).
# Descartáveis entre sessões; não são fonte nem regras.
eval.json
eval_result.json
evaluation.json
eval_logs*.json
evaluate_logs*.json
log_eval.json
log_traceability_eval.json
*_eval.json

# Python caches
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Coverage
.coverage
.coverage.*
htmlcov/
coverage.xml

# IDE
.vscode/
.idea/
*.swp

# Logs + ad-hoc debug scripts (use .tmp/ instead)
*.log
_inspect*.py
_debug*.py
_scratch*.py

# Build artifacts
*.egg-info/
build/
dist/

# Per-machine paths — commit models.conf.example template instead
models.conf

# Analyzer baseline cache (machine-local)
.uipath-rules-cache/
